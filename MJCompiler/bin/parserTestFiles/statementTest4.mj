program MJProgram
    const boolean flag = true;
    {
        void f() int num, a, b[]; {
            if (a > b) 
            {
                return 'o';
                if (flag) print(flag);
                else return 'n';
            }
            if (a > b) return;
            if (flag) return;
            else read(a);
        }
    }
