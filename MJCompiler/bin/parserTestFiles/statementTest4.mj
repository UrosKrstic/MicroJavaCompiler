program MJProgram
    const boolean flag = true;
    {
        void f() int num, a, b[]; {
            if (a > b) 
            {
                return "lol";
                if (flag) print(flag);
                else return "nope";
            }
            if (a > b) return;
            if (flag) return;
            else read(a);
        }
    }