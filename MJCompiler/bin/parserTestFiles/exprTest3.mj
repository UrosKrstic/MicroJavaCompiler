program MJProgram
    {
        void f() {
            return -x + a;
            return a + x.b[-a+2].s[4][(-new D * d) + foo(x,2 + z) * 'l'] * y / z - a;
            return;
            c = new int[5];
            b = f() + c[a + x];
        }
    }
